import React from 'react'

const Formfield = ({
    name,
    value,
    onChange,
    error,
    label,
    children,
    props,
    className
}) => {
  return (
    <div>
      <label></label>
    </div>
  )
}

export default Formfield
